[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect: serve index.html for missing routes, but Netlify will still serve static assets by filename first
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

